# Примеры использования N-Body Симуляции

## 1. Запуск симуляции

После сборки проекта (`make`), запустите исполняемый файл:

```bash
./bin/nbody_simulation
```

Вы увидите меню:

```
Select mode:
  1. Run Barnes-Hut simulation (Single Galaxy)
  2. Run Brute Force simulation (Single Galaxy)
  3. Benchmark Barnes-Hut only
  4. Benchmark Brute Force only
  5. Compare both algorithms (benchmark)
  6. Run Galaxy Collision (Barnes-Hut)
  0. Exit
```

## 2. Режимы визуализации

### Одиночная галактика (Режимы 1 и 2)
Симуляция одной спиральной галактики с массивной черной дырой в центре.
- **Режим 1 (Barnes-Hut)**: Быстрый алгоритм, подходит для большого количества частиц. Можно включить отображение дерева (клавиша `T`).
- **Режим 2 (Brute Force)**: Точный, но медленный алгоритм. Используйте для сравнения или с малым количеством частиц.

### Столкновение галактик (Режим 6) 🆕
Симуляция столкновения двух галактик.
- Две галактики (синяя и красная) летят навстречу друг другу.
- Каждая имеет свою черную дыру в центре.
- Используется алгоритм Barnes-Hut для производительности.
- Наблюдайте, как гравитация искажает их формы и выбрасывает "хвосты" из звезд!

## 3. Управление в окне визуализации

| Клавиша | Действие |
|---------|----------|
| `SPACE` | Пауза / Продолжить симуляцию |
| `T`     | Показать / Скрыть сетку дерева (только для Barnes-Hut) |
| `ESC`   | Выход в меню |

## 4. Бенчмаркинг (Режимы 3, 4, 5)

Используйте эти режимы для измерения производительности.
- Результаты сохраняются в текстовые файлы (`benchmark_*.txt`).
- Режим 5 автоматически запускает оба алгоритма и показывает сравнение скорости.

## 5. Настройка параметров

Вы можете изменить параметры симуляции в файле `include/Constants.h`:
- `NUM_STARS_EXCEPT_BH`: Количество звезд.
- `DT`: Шаг времени (меньше = точнее и стабильнее).
- `THETA`: Точность алгоритма Barnes-Hut.

После изменения констант не забудьте пересобрать проект:
```bash
make clean && make
```
