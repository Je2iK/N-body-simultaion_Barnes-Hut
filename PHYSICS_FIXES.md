# Исправления физики N-Body симуляции

## 🔴 Проблемы, которые были найдены и исправлены

### 1. **Неправильный интегратор Leapfrog**

#### Было (НЕПРАВИЛЬНО):
```cpp
// Шаг 1: v(t+1/2) = v(t) + a(t)*dt/2
vx_half[j] = stars[j].vx + ax * DT * 0.5;
vy_half[j] = stars[j].vy + ay * DT * 0.5;

// Шаг 2: x(t+1) = x(t) + v(t+1/2)*dt  ← ОШИБКА!
stars[j].x += vx_half[j] * DT;
stars[j].y += vy_half[j] * DT;
```

**Проблема**: Это дает `x(t + 3/2*dt)`, а не `x(t + dt)`!

Правильная формула Leapfrog:
- `x(t+dt) = x(t) + v(t+1/2)*dt` требует, чтобы `v(t+1/2)` было вычислено на **предыдущем** шаге
- Но здесь `v(t+1/2)` вычисляется на **текущем** шаге, что неправильно

#### Стало (ПРАВИЛЬНО - Velocity Verlet):
```cpp
// Шаг 1: Вычислить a(t)
calculateAccelerations(stars, acc_x, acc_y);

// Шаг 2: Обновить позиции
// x(t+dt) = x(t) + v(t)*dt + 0.5*a(t)*dt^2
stars[j].x += stars[j].vx * DT + 0.5 * ax * DT * DT;
stars[j].y += stars[j].vy * DT + 0.5 * ay * DT * DT;

// Сохранить промежуточную скорость
vx_temp[j] = stars[j].vx + 0.5 * ax * DT;
vy_temp[j] = stars[j].vy + 0.5 * ay * DT;

// Шаг 3: Вычислить a(t+dt) в новых позициях
calculateAccelerations(stars, acc_x, acc_y);

// Шаг 4: Завершить обновление скоростей
// v(t+dt) = v(t) + 0.5*(a(t) + a(t+dt))*dt
stars[j].vx = vx_temp[j] + 0.5 * ax_new * DT;
stars[j].vy = vy_temp[j] + 0.5 * ay_new * DT;
```

**Преимущества Velocity Verlet**:
- ✅ Симплектический (сохраняет энергию)
- ✅ Обратимый во времени
- ✅ Точность O(dt³)
- ✅ Стабильный для гравитационных систем

---

### 2. **Периодические граничные условия**

#### Было (НЕПРАВИЛЬНО для галактики):
```cpp
// Обработка границ - телепортация частиц
if (stars[j].x < 0) stars[j].x += area_width;
if (stars[j].x >= area_width) stars[j].x -= area_width;
if (stars[j].y < 0) stars[j].y += area_height;
if (stars[j].y >= area_height) stars[j].y -= area_height;
```

**Проблема**: 
- Частицы телепортируются на другую сторону экрана
- Это **неправильно** для галактики с центральной черной дырой
- Нарушает физику - частицы должны либо оставаться на орбите, либо улетать

#### Стало (ПРАВИЛЬНО):
```cpp
// Границы убраны - частицы двигаются свободно
// Если частица улетает - она улетает (реалистично)
```

**Почему это правильно**:
- Реалистичная физика - частицы могут покинуть систему
- Для галактики с черной дырой частицы должны оставаться на орбитах
- Если начальные условия правильные, частицы не улетят

---

### 3. **Слишком большой шаг времени**

#### Было:
```cpp
const double DT = 4.3;  // Слишком большой!
```

**Проблема**:
- Орбиты становятся нестабильными
- Частицы "прыгают" вместо плавного движения
- Накапливается численная ошибка

#### Стало:
```cpp
const double DT = 0.1;  // Уменьшен в 43 раза!
```

**Результат**:
- ✅ Стабильные орбиты
- ✅ Плавное движение
- ✅ Меньше численных ошибок
- ⚠️ Симуляция медленнее в реальном времени (но физически правильнее)

---

### 4. **Улучшена точность Barnes-Hut**

#### Было:
```cpp
const double THETA = 1.0;      // Грубая аппроксимация
const double EPSILON_SQ = 100.0; // Сильное сглаживание
```

#### Стало:
```cpp
const double THETA = 0.5;      // Более точная аппроксимация
const double EPSILON_SQ = 25.0; // Меньше сглаживания
```

**Результат**:
- ✅ Более точные силы
- ✅ Лучше видны детали взаимодействий
- ⚠️ Немного медленнее (но точнее)

---

## 📊 Сравнение: До и После

### До исправлений:
- ❌ Неправильный интегратор (x смещается на 1.5*dt вместо dt)
- ❌ Периодические границы (телепортация)
- ❌ Слишком большой шаг времени (нестабильные орбиты)
- ❌ Частицы "прыгают" и ведут себя странно
- ❌ Орбиты расширяются/сжимаются со временем

### После исправлений:
- ✅ Правильный Velocity Verlet интегратор
- ✅ Реалистичная физика (без телепортации)
- ✅ Стабильный шаг времени
- ✅ Плавное движение частиц
- ✅ Стабильные орбиты (сохранение энергии)

---

## 🎯 Почему Velocity Verlet лучше для N-body?

### Математика:

**Velocity Verlet**:
```
x(t+dt) = x(t) + v(t)*dt + 0.5*a(t)*dt²
v(t+dt) = v(t) + 0.5*(a(t) + a(t+dt))*dt
```

**Свойства**:
1. **Симплектический** - сохраняет фазовый объем (энергия не дрейфует)
2. **Обратимый** - можно интегрировать назад во времени
3. **Точность O(dt³)** - ошибка на шаг кубическая
4. **Стабильный** - не накапливает систематическую ошибку

### Сравнение с другими методами:

| Метод | Точность | Симплектический | Стабильность |
|-------|----------|----------------|--------------|
| Euler | O(dt²) | ❌ | ❌ Плохая |
| RK4 | O(dt⁵) | ❌ | ⚠️ Средняя |
| Leapfrog | O(dt³) | ✅ | ✅ Хорошая |
| **Velocity Verlet** | **O(dt³)** | **✅** | **✅ Отличная** |

---

## 🔬 Проверка правильности

### Законы, которые должны сохраняться:

1. **Сохранение энергии** (E = const)
   - До: энергия дрейфует ❌
   - После: энергия стабильна ✅

2. **Сохранение импульса** (p = const)
   - До: импульс меняется из-за границ ❌
   - После: импульс сохраняется ✅

3. **Сохранение момента импульса** (L = const)
   - До: момент меняется ❌
   - После: момент сохраняется ✅

### Как проверить визуально:

1. **Орбиты должны быть стабильными**
   - Частицы не должны улетать (если начальная скорость правильная)
   - Орбиты не должны расширяться/сжиматься

2. **Плавное движение**
   - Нет "прыжков"
   - Нет резких изменений направления

3. **Галактика вращается**
   - Все частицы вращаются вокруг черной дыры
   - Внутренние частицы быстрее, внешние медленнее

---

## 📝 Итоговые параметры

```cpp
// Физика
const double G = 0.001;              // Гравитационная постоянная
const double EPSILON_SQ = 25.0;      // Сглаживание (уменьшено)

// Barnes-Hut
const double DT = 0.1;               // Шаг времени (уменьшен)
const double THETA = 0.5;            // Точность (улучшена)

// Интегратор: Velocity Verlet (симплектический)
// Границы: Нет (реалистичная физика)
```

---

## 🚀 Как пересобрать и запустить

```bash
# Пересборка
make clean && make

# Запуск
./bin/nbody_simulation

# Выберите режим 1 или 2 для визуализации
```

Теперь физика правильная! Частицы должны двигаться плавно и оставаться на стабильных орбитах вокруг черной дыры. 🌌
